// Init SpeechSyth API
const syth = window.speechSynthesis;

// DOM Elements
const textform = document.querySelector('form');
const tex tInput = document.querySelector('form');
const textform = document.querySelector('form');
const textform = document.querySelector('form');
const textform = document.querySelector('form');
const textform = document.querySelector('form');
const textform = document.querySelector('form');
